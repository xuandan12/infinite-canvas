{
    "framework": "vite",
    "installCommand": "bun install",
    "buildCommand": "bun run build",
    "outputDirectory": "dist",
    "functions": {
        "api/ark/**/*.ts": { "maxDuration": 60 }
    },
    "rewrites": [
        {
            "source": "/api/ark/:path*",
            "destination": "/api/ark/:path*"
        },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}
